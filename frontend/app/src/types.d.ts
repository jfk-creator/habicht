interface registration_t {
  email: string;
  secret: string;
  address_name: string;
  city_code: string;
  city_name: string;
  street_name: string;
  street_number: string;
}

interface LoginPackage {
  email: string;
  secret: string;
}
